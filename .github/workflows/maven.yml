name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    services:
      redis:
        image: redis
        ports:
          - 6379:6379
        options: --entrypoint redis-server

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        working-directory: ./ECS-160-WQ-25/HW 1
        run: mvn clean package  # Assuming the pom.xml is in the root

      - name: Run Tests
        working-directory: ./ECS-160-WQ-25/HW 1
        run: mvn test

      - name: Create Sample Input JSON
        working-directory: ./ECS-160-WQ-25/HW 1
        run: |
          echo '{"posts": [{"id": 1, "content": "Hello world!", "replies": []}]}' > sample_input.json

      - name: Run the Application
        working-directory: ./ECS-160-WQ-25/HW 1
        run: java -jar target/HW1-solution-1.0-SNAPSHOT.jar --weighted false --file "./sample_input.json"
